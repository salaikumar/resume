import React, {Component} from 'react';

var ResumeContent = React.createClass({
    render(){
        return(
            <div>
                ResumeContent
            </div>
        )
    }
});

export default ResumeContent;